#include<stdio.h>
int main()
{
	int x[5],i,max,min;
	for(i=0;i<5;i++)
	{
		scanf("%d",&x[i]);
	}
	max=x[0];
	min=x[0];
	for(i=0;i<5;i++)
	{
		if(x[i]>max)
		max=x[i];
		if(x[i]<min)
		min=x[i];
	}
	printf("maximum number:%d and minimum number:%d",max,min);
}
